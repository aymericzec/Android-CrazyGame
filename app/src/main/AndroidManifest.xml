<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.upem.crazygame">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_GPS" />
    <uses-permission android:name="android.permission.ACCESS_ASSISTED_GPS" />
    <uses-permission android:name="android.permission.LOCATION" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".searchgameactivity.SearchGameActivity">
            <intent-filter>
                <!--
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="vnd.android.cursor.item/vnd.tutos.android.content.provider.cours" />

            </intent-filter>
        </activity>

        <provider android:name=".chat.AndroidProvider"
            android:authorities="com.tutos.android.content.provider.tutosandroidprovider" />


        <activity android:name=".game.morpion.MorpionActivity"></activity>
        <activity android:name=".game.mixwords.MixWordActivity"></activity>
        <service android:name=".chat.chatService"/>

        <activity android:name=".chat.ContentProviderExempleActivity"></activity>


        <receiver class="MessageReceiver"
            android:name=".chat.MessageReceiver">
            <intent-filter android:priority="100">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>